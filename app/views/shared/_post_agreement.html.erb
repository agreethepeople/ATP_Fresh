
A FORM HERE FOR ADDING AN AGREEMENT

<%= form_for(@topic.agreements.build) do |f| %>
	<div class="field">
		<%= f.text_area :content, placeholder: "#{current_user.name} Don't see what you're looking for in #{@topic.title}? Write your own agreement..." %>
	</div>
	<div><%= f.hidden_field :user_id, value: current_user.id %></div>
	<div><%= f.hidden_field :topic_id, value: @topic.id %></div>
	<%= f.submit "Submit", class: "btn btn-large btn-primary" %>
<% end %>